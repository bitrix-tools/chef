BASE_URL=
LOGIN=
PASSWORD=

# Additional users for multi-user e2e tests (optional).
# Add a LOGIN_<ID> / PASSWORD_<ID> pair per user, then select them in a test with
# test.use({ user: 'manager' }) or await loginAs('manager').
# LOGIN_MANAGER=
# PASSWORD_MANAGER=
